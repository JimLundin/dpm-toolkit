[project]
name = "analysis"
version = "0.1.0"
description = "Meta-analysis tool for discovering type refinement opportunities in DPM databases"
requires-python = ">=3.13"
dependencies = [
    "sqlalchemy>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
